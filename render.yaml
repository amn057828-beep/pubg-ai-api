services:
  - type: web
    name: pubg-ai-api
    env: docker
    rootDir: backend
    plan: free
    envVars:
      - key: ENV
        value: production
      - key: OPENAI_API_KEY
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: DATABASE_URL
        sync: false
